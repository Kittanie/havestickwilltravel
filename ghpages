LIVE:

git init
git checkout --orphan gh-pages
git branch
git add -A
git commit -m "gh-pages sync"
create new repo on github - git remote
git push origin gh-pages
git branch


URL: http://kitto1.github.io/havestickwilltravel

---------------------------------------
LOCAL:

Navigate to website folder
run command "ecstatic"
listen on poer 8000

URL: localhost:8000 or 192.168.1.5:8000

CTRL + C to stop running local
---------------------------------------